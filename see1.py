def see1(word, phrase):
    see1 = "hi hi hello"

    word_size = len(word)
    phrase_size + len(phrase)
    count = 0
    for i in range(0, word_size-phrase_size+1):
        if string[i:i+phrase_size] == phrase:
            count +=1
    return count
    print(count)


    
